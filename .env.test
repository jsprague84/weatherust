# Test configuration for updatemon
# Copy relevant values from your .env and add test servers

# Gotify settings (for notifications)
GOTIFY_URL=https://gotify.js-node.com/message
GOTIFY_KEY=AF__jT.HvVvTPbm
GOTIFY_DEBUG=true

# Test servers (update with your actual server addresses)
# Format: name:user@host,name:user@host
# Examples:
#   UPDATE_SERVERS=docker-vm:ubuntu@192.168.1.x,fedora-ws:user@192.168.1.y
#   UPDATE_SERVERS=docker-vm:ubuntu@docker-vm
UPDATE_SERVERS=

# SSH key path (if you use a specific key)
# UPDATE_SSH_KEY=/home/jsprague/.ssh/id_ed25519
